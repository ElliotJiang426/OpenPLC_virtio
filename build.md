```sh
apt update
apt upgrade
apt install -y git python3 python3-pip cmake 
apt upgrade -y bison yacc flex 
git clone https://github.com/thiagoralves/OpenPLC_v3.git
cd OpenPLC_v3
./install.sh docker
```